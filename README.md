# Serpentine

Interim bootstrap for the `serpentine` repository.

This initial push exists so GitHub Pages can be enabled while the full library extraction and demo are still being prepared.

Expected Pages URL after GitHub Pages is enabled for the `main` branch and `/docs` folder:

- `https://luckymonkey.github.io/serpentine/`

The real library source, documentation, tests, and demo will be pushed on top of this branch next.
