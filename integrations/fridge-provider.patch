--- a/index.php
+++ b/index.php
@@ -146,12 +146,17 @@
     $host = preg_replace('#^https?://#i', '', $link);
     $host = preg_replace('#/.*$#', '', $host);
     $host = preg_replace('#:\d+$#', '', $host);
+    $favicon = (string) ($entry['favicon'] ?? '');
+    if (!preg_match('#^/public/favicons-[a-z0-9-]+/[a-zA-Z0-9.-]+\.(png|svg|jpg|ico)$#D', $favicon) || !is_readable(__DIR__ . $favicon)) {
+        $favicon = fetch_favicon_data_uri($host);
+    }
     return [
         'name' => (string) $entry['name'],
         'link' => $link,
         'host' => $host,
         'url' => display_link_url($link),
-        'favicon' => fetch_favicon_data_uri($host),
+        'favicon' => $favicon,
+        'description' => (string) ($entry['description'] ?? ''),
         'accent' => host_accent_color($host),
     ];
 }, $links);
@@ -175,8 +180,6 @@
   <meta charset="utf-8">
   <meta name="viewport" content="width=device-width, initial-scale=1">
   <title>fridge.local</title>
-  <link rel="preconnect" href="https://www.google.com">
-  <link rel="preconnect" href="https://www.gstatic.com" crossorigin>
   <style>
     :root {
       color-scheme: light;
@@ -355,23 +358,23 @@
       background: transparent;
     }
   </style>
-  <link rel="stylesheet" href="/public/serpentine-c6a1abd.css">
+  <link rel="stylesheet" href="/public/serpentine-ui1.css">
 </head>
 <body>
   <script id="homepage-links" type="application/json"><?= json_encode($normalized, JSON_UNESCAPED_SLASHES | JSON_UNESCAPED_UNICODE | JSON_HEX_TAG | JSON_HEX_APOS | JSON_HEX_AMP | JSON_HEX_QUOT) ?></script>
   <script id="homepage-config" type="application/json"><?= json_encode($testConfig, JSON_UNESCAPED_SLASHES | JSON_UNESCAPED_UNICODE | JSON_HEX_TAG | JSON_HEX_APOS | JSON_HEX_AMP | JSON_HEX_QUOT) ?></script>
-  <div class="static-links" aria-hidden="true">
+  <div class="static-links">
     <?php foreach ($normalized as $item): ?>
       <a class="static-card" href="<?= htmlspecialchars($item['link'], ENT_QUOTES, 'UTF-8') ?>" style="--accent: <?= htmlspecialchars($item['accent'], ENT_QUOTES, 'UTF-8') ?>;">
         <span class="icon-well" aria-hidden="true"><img class="favicon" src="<?= htmlspecialchars($item['favicon'], ENT_QUOTES, 'UTF-8') ?>" alt=""></span>
         <div class="copy">
           <p class="name"><?= htmlspecialchars($item['name'], ENT_QUOTES, 'UTF-8') ?></p>
-          <p class="url" title="<?= htmlspecialchars($item['link'], ENT_QUOTES, 'UTF-8') ?>"><?= htmlspecialchars($item['url'], ENT_QUOTES, 'UTF-8') ?></p>
+          <p class="url" title="<?= htmlspecialchars($item['link'], ENT_QUOTES, 'UTF-8') ?>"><?= htmlspecialchars($item['description'] ?: $item['url'], ENT_QUOTES, 'UTF-8') ?></p>
         </div>
       </a>
     <?php endforeach; ?>
   </div>
   <div id="serpentine-root"></div>
-  <script defer src="/public/serpentine-c6a1abd.js"></script>
+  <script defer src="/public/serpentine-ui1.js"></script>
 </body>
 </html>
